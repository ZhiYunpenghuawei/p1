# vLLM-GR 在线推理调度策略：调研与设计文档

> 状态：调研阶段（v0.1）
> 适用范围：vllm-gr（vLLM-based Generative Recommender Inference Engine，GPU / NPU 双平台）
> 关联场景：OneRec beam-search / catalog-constrained generation、RECIF 多 token 生成（默认 3 个 decode 阶段）

---

## 1. 背景与问题定义

### 1.1 我们面对的场景

- **在线推理**：请求在时间上连续到达，无法预先知道完整请求集合。
- **长度未知**：prefill 长度（context 长度）在请求到达前不可知；decode 步数对 RECIF 固定（3 token），但对其他 GR 模型可变。
- **成图要求**：为了降低 kernel launch 与 Python/调度开销，需要把模型 forward 捕获为 CUDA Graph（GPU）或 ACL Graph（NPU）。
- **调度与成图强耦合**：图要求“形状静态 + 内存地址稳定”，而调度决定了每一步的 batch 组成、形状分布和 KV 分配，直接决定图命中率、padding 浪费和图数量。

### 1.2 需要回答的问题

1. 调度策略应该采用“调用方组好 batch、固定步长一起走”（fixed-step batching），还是“请求进来就接入”（continuous batching）？
2. 成图有哪些层次？哪些可行、哪些不可行？
   - 每个 decode 阶段一张图（现状）；
   - 全流程（prefill + 所有 decode）一张图；
   - prefill 单独成图；
   - 同阶段多个并行 decode 合一张图；
   - 不同阶段（decode1 / decode2）的请求合一张图。
3. prefill 成图是否应该采用“长度 bucket 预分配”（如 1024 的 bucket 放 900/1000）？
4. 不组 batch、长度随机到达时，能否成图？
5. 综合成图与调度，性能如何排序、后续怎么实施？

### 1.3 文档结论摘要（TL;DR）

1. **不二选一，分层设计**：引擎侧采用**连续接入**（即来即接），执行侧采用**阶段分桶**（同 decode 阶段、同形状的请求合批），即 NVIDIA GR continuous batching 的路线。调用方组 batch 的固定步长模式仅作为**可选高吞吐模式**保留，用于 A/B 对照和特定流量。
2. **成图层次可行性**：
   - 每 decode 阶段一张图：✅ 可行（现状）；
   - 同阶段并行 decode 一张图：✅ 可行（NVIDIA 已实现，按 batch bucket 填充）；
   - prefill 单独成图：✅ 可行（NVIDIA 已实现，按长度 bucket + piecewise 捕获）；
   - 跨阶段共一张图：❌ 不做（图 key 含阶段，阶段间逻辑不同）；
   - 全流程一张图：⚠️ 原则上仅对固定步数+无 host 分支可行，复杂度/风险极高，一期不做。
3. **性能预期排序**（综合吞吐/延迟/成图收益）：
   连续接入 + 阶段分桶 + 图 bucket（推荐） ＞ 固定步长批处理（高流量下接近） ＞ 连续接入无分组（当前基线） ＞ 纯静态批处理。

---

## 2. 现状代码事实（调研依据）

以下事实来自指定 commit 的源码（已全部抓取验证）：

- vllm `vllm/v1/core/sched/scheduler.py`（commit `52be12c`，L438–L790）
- vllm `vllm/v1/engine/core.py`（commit `52be12c`，L580–L608）
- vllm `vllm/entrypoints/generate/beam_search/offline.py`（commit `52be12c`，L210–L275）
- NVIDIA `recsys-examples/.../gr_serving/continuous.py`（commit `9a3bf5d`，6 个段落）及引用的 `decode_cuda_graph.py`、`prefill_cuda_graph.py`
- xllm `rec_master.cpp` / `fixed_steps_scheduler.cpp` / `rec_engine.cpp` / `rec_worker_impl.cpp`（commit `cd167a6`，共 7 段）

### 2.1 vllm：纯连续批处理（即来即接）

`scheduler.schedule()` 每步执行：

1. 先调度 `running`（decode 续跑）请求，直到 token 预算耗尽；
2. 再调度 `waiting`（prefill）请求，容量允许即接入；
3. KV 不足时抢占（PRIORITY 策略抢优先级最低者，否则弹栈 FCFS）；
4. 支持 chunked prefill（`long_prefill_token_threshold`）、prefix cache、spec decode 槽位、PP 异步步进节流（`next_decode_eligible_step`）。

`core.step()` 主循环为：`schedule() → execute_model(non_block) → sample → update_from_output()`。

结论：vllm 默认就是“请求进来就接入”，调度不感知 decode 阶段，也不为成图做形状分组。vllm 的 CUDA graph 只按 batch 大小分桶，长度作为数据传入。

### 2.2 NVIDIA GR：连续接入 + 阶段分组执行 + 图桶填充

`GRContinuousScheduler.tick()`：

1. 每个 tick 从 `waiting_prefill` 接入最多 `max_prefill_batch_size` 个请求（**连续接入**）；
2. `_plan_decode_batches()`：把 decoding 中的请求按 **`(current_decode_step, beam_width, next_beam_width, context_len)`** 分组，每组切成不超过 `max_decode_batch_size` 的 microbatch（**阶段分组**，不同阶段不混批）；
3. `_run_decode_batch()`：同一 microbatch 内做 batch 化的 beam 输入/KV/topk 组装，一次 `forward_decode_step`；
4. decode CUDA graph（`GRDecodeCudaGraphRunner`）：
   - 图 key 包含：device、dtype、`beam_token_ids.shape`、`context_len`、context/beam KV 的 shape 与 **view key（地址/偏移）**、`step`、`active_beam_width`、`decode_nums`；
   - 复用条件：KV 池切片 data_ptr 与 storage_offset 完全一致（`_reuse_miss_reason`），否则 pointer miss → 重捕获或退回 eager；
   - 实际 batch 小于 bucket 时，用预分配的 KV 池切片 padding 到 `decode_cuda_graph_batch_buckets`，replay 后把 logits 切回实际 batch 大小；
   - 需要“稳定池窗口”（context/beam KV 都是池切片）才能用图。
5. prefill CUDA graph（`GRPrefillCudaGraphRunner`）：
   - 图 key 包含 `input_ids.shape`、context KV shape/view、`last_token_logits_only`；
   - 默认 **piecewise 捕获**：embed 一张图 + 每若干层一张图 + output 一张图，降低 capture 内存；
   - 也支持 full（整网一张）模式。

结论：NVIDIA 证明了“连续接入 + 阶段分组 + 图桶填充”的完整可行路径，且 prefill 图与 decode 图都已落地。

### 2.3 xllm：固定步长批处理（调用方组 batch）

`FixedStepsScheduler::prepare_batch()`：

1. 从 request queue 取出新请求，无 prefix cache 时展开 beam；
2. 在 `max_tokens_per_batch` / `max_seqs_per_batch` 预算内做 prefill 调度（`handle_prefill_requests`）；
3. 按 Rec 类型由 pipeline 生成 batch（`OneRec / OneRecXAttention / LlmRec / MultiRound`）；
4. `OneRecPrefillOnlyEnginePipeline::step()`：**一次 prefill forward + 连续 `kRecDecodeSteps` 次 decode forward**，整批从头到尾不换人；
5. XAttention 变体把多 round 放在同一个 engine step 内循环（内部仍是逐 round 发 kernel，beam_search 在 round 间作为独立 kernel 执行）。

结论：xllm 是“固定步长”的代表实现——批量一起进、一起走完固定 decode 步。它的成图方式未在本批链接中展开（图捕获在 Executor 层），但其固定形状特性天然利于按 batch 成图。

### 2.4 本地 vllm-gr 现状

- 仓库是 vllm 的 fork（`vllm_gr/`），引擎内核仍是 vllm 连续调度。
- RECIF 入口把“prefill + 全部 beam 后缀”打包成一个 Beam Request（`vllm_gr/v1/engine/core.py` L195–L227），带 `is_beam_decode` 标记和 `beam_decode_steps`（即阶段数，RECIF 为 3）。
- NPU 侧已有 ACL graph 参数更新回调（`vllm_gr/v1/attention/backends/beam_attn_npu.py`），说明 NPU 成图基础设施已在建设中。
- 当前调度器**没有**按 decode 阶段分组的逻辑，也没有为成图做 bucket 分组。

---

## 3. 调度策略全景与筛选

### 3.1 全景清单

| # | 策略 | 一句话描述 | 代码出处 |
|---|------|-----------|---------|
| 1 | 静态批处理 | 离线定 batch，生成期间不进出请求 | vllm offline beam search |
| 2 | 连续批处理 | 每步重新调度，容量允许即接入 | vllm scheduler |
| 3 | 固定步长批处理 | 一批一起走完固定 decode 步 | xllm FixedStepsScheduler |
| 4 | 阶段分组 | 同 decode 阶段同形状合批 | NVIDIA `_plan_decode_batches` |
| 5 | 图桶调度 | batch/长度向上取整到 bucket，padding 后成图 | NVIDIA graph runners |
| 6 | FCFS | 先来先服务 | vllm 默认 |
| 7 | 优先级调度 | 高优先级先跑，低优先级被抢占 | vllm `SchedulingPolicy.PRIORITY` |
| 8 | 短任务优先（SJF/SRPT） | 长度未知时难以直接使用 | 文献方案，不在此列 |
| 9 | 抢占（swap/recompute） | KV 不足时换出或重算 | vllm `_preempt_request` |
| 10 | Chunked prefill | 长 prefill 分块与 decode 交错 | vllm |
| 11 | Prefix-cache-aware | 共享前缀合批、跳过已算 KV | vllm |
| 12 | Spec decode 调度 | 预留 draft/verify 槽位 | vllm（一期不做） |
| 13 | PD 分离 | prefill/decode 分实例 | vllm / xllm disagg（一期不做） |
| 14 | 异步/重叠调度 | 调度与执行、PP 步进重叠 | vllm async / xllm overlap |

### 3.2 筛选原则

- 只推荐“能在当前 vllm-gr 架构上短期落地、且与成图目标一致”的策略；
- 排除需要大规模架构改造（PD 分离、spec decode）或收益不确定（SJF、全流程单图）的选项；
- 策略之间是**分层组合**关系，不是互斥关系。

---

## 4. 推荐策略（本阶段可落地）

### 4.1 策略一（主）：连续接入 + 阶段分桶执行

**一句话**：引擎保持 vllm 的“即来即接”，但调度输出按 decode 阶段分组执行；不同阶段的请求绝不在同一个模型调用里混跑。

**设计要点**：

1. 请求到达即进入 waiting 队列（连续接入，保持 vllm 语义）；
2. 调度器为每个请求维护 `decode_stage`（本地已有 `beam_decode_steps` / `is_beam_decode`）；
3. 每步输出按 `(decode_stage, beam_width, context_len)` 聚合成多个执行组：
   - prefill 组：新接入请求（按形状进一步分组，见 4.3）；
   - decode-stage-k 组：处于第 k 阶段的请求；
4. 每组独立执行，组内 batch 大小受 `max_decode_batch_size` 约束；
5. 组内请求完成本阶段后，更新 stage，下步进入下一阶段组；
6. 阶段分组不影响 token 预算与 KV 分配（仍受 `max_num_batched_tokens` 约束）。

**为什么是它**：

- 在线延迟最优：请求无需等待攒批；
- 成图友好：同组形状一致，图数量 = 阶段数 × bucket 数 × beam 宽，可控；
- 有现成参考：NVIDIA 已验证完整实现；
- 与 vllm 内核改动最小：在 scheduler 输出层增加分组，不重写调度内核。

### 4.2 策略二（配套）：图感知桶调度

**一句话**：为 CUDA/ACL 图把 batch 大小与 prefill 长度向上取整到预设 bucket，用预分配的 KV 池切片 padding，replay 后切回实际大小。

**设计要点**：

1. **decode batch bucket**：按流量统计选择少量 bucket，如 `1/2/4/8/16`；
2. **prefill 长度 bucket**：如 `512/1024/2048`（依据线上 context 长度分布）;
3. **KV 稳定池**：KV 分配器为每个 `(bucket, stage)` 预留固定 slot 窗口，保证同一形状的请求落到**相同地址**的池切片（避免 pointer miss）；
4. **padding 记账**：记录 padding 的请求数、slot 数、浪费比例，作为调参依据；
5. **输出切回**：图输出写共享 logits buffer，replay 后按实际 batch 切分（参考 NVIDIA `_slice_tensor_batch`）。

**注意**：bucket 数量不要贪多。每多一个 bucket = 多一份图私有池内存 + 一次 capture 成本。推荐从 3–5 个 decode bucket、2–3 个 prefill bucket 起步。

### 4.3 策略三（可选模式）：固定步长批处理

**一句话**：作为策略一的“高吞吐子模式”，允许调用方声明 batch 组，引擎把组内请求绑定为一批，一起 prefill、一起走完固定 decode 步（xllm 路线）。

**设计要点**：

1. 协议层增加可选 `batch_hint`（组 ID），同一组内请求只允许相近长度；
2. 调度器收到组内最后一个请求（或超时）才放行整组；
3. 整组从 prefill 到最后一个 decode 步保持在同一执行组内；
4. 组间仍按连续接入调度（不同组可以并行运行，占用不同 KV 窗口）。

**启用条件**（满足才开）：

- 流量稳定且到达率足够高（否则等待攒批增加延迟）；
- 调用方知道长度分布（能组相近长度）；
- 延迟 SLO 允许等待一个批窗口。

**一期建议**：作为 A/B 对照实现，不作为默认。

### 4.4 策略四（增强项）：优先级 + 抢占

**一句话**：默认 FCFS，可选 priority；KV 不足时按策略抢占，被抢占请求回 waiting 后按原 stage 重新接入。

**设计要点**：

1. 调度策略开关：`FCFS` / `PRIORITY`（vllm 已有）；
2. 抢占方式：swap（KV 换出）与 recompute（丢弃重算）二选一，按显存/时间权衡；
3. **与成图兼容**：被抢占请求重新接入时必须回到原 `(stage, bucket)`，且 KV 重分配尽量落到原池窗口，否则图命中下降；
4. 提供 metrics：抢占次数、被抢占请求的图 miss 次数。

### 4.5 明确不做 / 后置项

| 项 | 原因 | 状态 |
|---|------|------|
| 全流程一张图（prefill+全部 decode） | 依赖 host 分支（采样、停止、beam 重排、cache select），图无法捕获分支；仅固定步数+全静态可能，复杂度/风险极高 | 后置研究 |
| 跨阶段共享一张图 | 图 key 含 stage；阶段间 KV 写入位置、decode_nums、beam/cache 选择不同 | 不做 |
| 纯静态批处理作为在线默认 | 延迟与利用率差 | 不做（offline 保留） |
| PD 分离 | 需要多实例架构改造 | 一期不做 |
| Spec decode | 需要额外 draft 模型与槽位预算 | 一期不做 |
| SJF/SRPT 类 | 长度未知，预估误差大 | 不做 |

---

## 5. 成图设计（与调度策略配套）

### 5.1 硬约束

图 replay 要求：

1. **形状静态**：捕获时所有 kernel 的形状在 replay 时完全一致（batch、sequence、KV 切片尺寸）；
2. **地址稳定**：
   - 捕获期间的中间张量位于图私有内存池，地址固定；
   - 输入需拷贝进静态输入 buffer，输出需从静态输出 buffer 读回；
   - KV 池切片地址必须稳定（data_ptr + storage_offset 一致），否则 pointer miss → 重捕获或 eager 回退；
3. **无 host 分支**：捕获期间执行的代码路径在 replay 时完全一致（不能有 if/采样/停止判断在宿主侧改变路径）。

NPU/ACL 差异：

- 同样要求形状/内存大小固定；
- 但允许通过“参数更新回调”（如本地 `beam_attn_npu.py`）在 replay 前更新部分指针/参数，对地址稳定性约束略宽松；
- capture 需要 `DeviceCaptureLock` 防并发捕获互扰（xllm 已踩过坑）。

### 5.2 decode 图

**图 key**（建议与 NVIDIA 对齐）：

```
(device, dtype, batch_bucket, stage, beam_width, next_beam_width,
 context_len, context_kv_shape, beam_kv_shape, kv_view_key, decode_nums)
```

**执行流程**：

1. 调度器按阶段分组产出 microbatch；
2. 实际 batch → 找 bucket（向上取整）；
3. 用 KV 池切片 padding 到 bucket（context/beam 都要有稳定切片）；
4. 拷贝 `beam_token_ids` / `topk_indices` 进静态输入 buffer；
5. 图 replay → logits 写共享输出 buffer；
6. 按实际 batch 切回 logits；
7. 记录 miss reason（shape / context_key / beam_key / pool_window 等）。

### 5.3 prefill 图

**两种方案**：

1. **长度 bucket 方案**（推荐起步）：`input_ids` 形状固定为 `[B, bucket_len]`，900/1000 都 padding 到 1024；
   - 优点：实现简单、与 NVIDIA 对齐；
   - 代价：padding token 的 attention 计算浪费（900→1024 约 12%，1000→1024 约 2.4%，取决于分布）。
2. **max-token 单图方案**（更 vllm 的做法）：捕获一张按 `max_num_batched_tokens` 预算的图，真实长度作为数据传入 attention metadata；
   - 优点：无需长度 bucket、无 padding 浪费；
   - 前提：prefill kernel 支持变长（flattened tokens + attention metadata）。

**捕获方式**：默认 piecewise（embed 图 + 每 4 层一张图 + output 图），降低 capture 内存与失败率；整网图作为 fallback。

### 5.4 成图层次可行性总表

| 层次 | 可行性 | 依据 | 代价/风险 |
|---|------|------|----------|
| 每 decode 阶段一张图 | ✅ 可行（现状） | NVIDIA key 含 step；本地已逐阶段成图 | 图数 = 阶段 × bucket |
| 同阶段并行 decode 一张图 | ✅ 可行 | NVIDIA 同 (step, beam, ctx_len) 合批 + bucket padding | padding 浪费、需稳定池 |
| 不同阶段请求一张图 | ❌ 不做 | key 含 stage；阶段间逻辑不同 | — |
| prefill 单独成图 | ✅ 可行 | NVIDIA full/piecewise 均已实现 | 长度 bucket 或需变长 kernel |
| 全流程一张图 | ⚠️ 后置研究 | 需固定步数 + 无 host 分支 + 全静态 | 高复杂度、极脆 |

---

## 6. 后续实施路线图

> 周期为估计值，按双人并行、CUDA 先行 NPU 跟进估算。

### Phase 0：基线与指标（约 1–2 周）

- 建立 benchmark：吞吐（req/s、token/s）、FTT、TBT、KV 利用率、图命中率、capture 内存；
- 抓线上/压测流量分布：context 长度分布、阶段并发分布、到达间隔；
- 确定 bucket 初值（依据分布，先给 3–5 个）。

**验收**：基线数据可复现，指标采集埋点齐全。

### Phase 1：阶段分桶调度（核心，约 2–3 周）

- 在 vllm scheduler 输出层增加 stage 分组（复用 `beam_decode_steps`）；
- prefill 与各 decode stage 分组执行；组内顺序执行、组间可并行提交；
- 保持 token 预算/KV 分配语义不变。

**验收**：同一批请求中不同阶段的请求不再混跑；单测 + 正确性对比基线通过。

### Phase 2：decode 图 bucket + KV 稳定池（约 3–4 周）

- decode 图 runner（CUDA 先做，ACL 跟进）：
  - 图 key、padding、共享 logits buffer、miss 统计；
- KV 分配器支持稳定池窗口（按 bucket 预留 slot 区）；
- 动态跳过逻辑（pool 不稳定、padding 池不足时回退 eager）。

**验收**：命中率 ≥ 90%（典型负载）；padding 浪费比例有账可查；TBT 相比 eager 下降。

### Phase 3：prefill 图（约 2–3 周）

- 先做长度 bucket + piecewise 捕获（CUDA）；
- 再评估 max-token 单图（若 kernel 支持变长则替换）；
- NPU 侧 ACL prefill 图。

**验收**：prefill 图命中率与 FTT 改善可量化；capture 内存受控。

### Phase 4：固定步长模式（可选，约 2 周）

- 协议增加 `batch_hint`；调度器组批放行；
- 与 Phase 1+2 组合做 A/B：吞吐、FTT/TBT、图命中率对比。

**验收**：给出“何时启用”的数据结论（到达率/长度分布阈值）。

### Phase 5：优先级与抢占增强（约 2 周）

- PRIORITY 策略与抢占回接 stage 语义；
- 抢占后图 miss 统计与优化。

**验收**：高优请求延迟达标；抢占不显著降低图命中率。

### Phase 6：调优与压测（持续）

- bucket 数量、padding 比例、capture 预热、图 LRU 上限调参；
- 多模型（RECIF 3-token / 更长 decode / catalog-constrained）压测。

---

## 7. 预期性能排序

> 相对值（以“纯连续接入、无分组、无图”为基线 1.0），为目标方向而非实测承诺。

| 策略组合 | 吞吐 | FTT（首 token 延迟） | TBT（decode 步长） | 图命中率 | padding 浪费 | 实现成本 |
|---|---|---|---|---|---|---|
| S1+S2：连续接入 + 阶段分桶 + 图 bucket | ★★★★★ | ★★★★★ | ★★★★★ | 高 | 中 | 中高 |
| S3：固定步长（高流量下） | ★★★★★ | ★★★ | ★★★★ | 高 | 中 | 中 |
| S1 无 bucket：阶段分桶 + eager | ★★★★ | ★★★★ | ★★★ | 低 | 0 | 低 |
| 纯连续接入（当前 vllm 基线） | ★★★★ | ★★★★ | ★★★ | 低 | 0 | 0 |
| 纯静态批处理 | ★★ | ★★ | ★★★ | 中 | 高 | 低 |

**判断口径**：

- **吞吐**：S1+S2 与 S3 在“满负载、形状集中”时接近；S3 在稀疏流量下明显下降；
- **延迟**：S1 系（连续接入）最优；S3 引入攒批等待；
- **成图收益**：TBT 的收益来自 decode 图（launch 开销占比高）；FTT 的收益来自 prefill 图（占比相对小，prefill 是计算密集）；
- **图数量/内存**：阶段 × bucket × beam 是主要开销，因此 bucket 数必须克制。

---

## 8. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 图私有池内存膨胀 | 显存压力 | 控制 bucket 数；图 LRU 上限；piecewise 捕获；监控 capture 内存 |
| capture 抖动（首次见新形状） | 单步延迟尖峰 | 启动时按 bucket 预热；生产前用代表形状跑 warmup |
| padding 浪费 | 有效吞吐下降 | 按流量分布选 bucket；启用变长 kernel 后改 max-token 单图 |
| 阶段分组后 batch 利用率下降 | 吞吐损失 | `max_decode_batch_size` 调优；必要时启用 S3 组批 |
| KV 池不稳定导致 pointer miss | 图命中率下降 | 稳定池窗口设计；miss reason 埋点 |
| NPU ACL 捕获并发互扰 | capture 失败/卡顿 | DeviceCaptureLock；捕获与执行流分离 |
| 优先级抢占破坏图复用 | 命中率下降 | 被抢占请求重新分配回原池窗口 |

---

## 9. 参考代码位置

- vllm 连续调度：`vllm/v1/core/sched/scheduler.py` L438–L790
- vllm 引擎主循环：`vllm/v1/engine/core.py` L580–L608
- vllm offline 调用方组批：`vllm/entrypoints/generate/beam_search/offline.py` L210–L275
- NVIDIA 连续接入 + 阶段分组：`continuous.py` `GRContinuousScheduler.tick` / `_admit_prefill_batch` / `_plan_decode_batches`（L270–L434、L515–L545）
- NVIDIA decode 图：`decode_cuda_graph.py`（key / padding / pointer reuse）
- NVIDIA prefill 图：`prefill_cuda_graph.py`（length key / piecewise / full）
- xllm 固定步长：`fixed_steps_scheduler.cpp` L193–L321；`rec_engine.cpp` L699–L759（OneRec prefill + kRecDecodeSteps）
- 本地现状：`vllm_gr/v1/engine/core.py` L195–L227（Beam Request / decode stage）；`vllm_gr/v1/attention/backends/beam_attn_npu.py`（ACL 参数更新）

---

## 10. 附录：指标定义

| 指标 | 定义 |
|---|---|
| FTT（首 token 延迟） | 请求接入到第一个生成 token 输出的时间 |
| TBT（token 间延迟） | decode 相邻两步的间隔（含调度/执行/同步） |
| 图命中率 | 图命中次数 / 总 forward 次数 |
| pointer miss 率 | 形状命中但地址不匹配的占比 |
| padding 浪费 | padding token 数 / 图内总 token 数 |
| capture 内存 | 图私有内存池 + 共享输出 buffer 总量 |
| KV 利用率 | 已用 KV 块 / 总 KV 块 |
| 抢占次数 | 每策略单位时间抢占请求数 |
