# vLLM-GR 在线推理调度策略：三库调研与设计方案

> 状态：调研 + 方案设计（v0.2）
> 适用范围：vllm-gr（vLLM-based Generative Recommender Inference Engine，GPU / NPU 双平台）
> 关联场景：OneRec beam-search / catalog-constrained generation、RECIF 多 token 生成（默认 3 个 decode 阶段）

---

## 0. 调研对象与可获取性声明

本版文档基于三个代码库展开：

| 代码库 | 可获取性 | 分析依据 |
|---|---|---|
| NVIDIA `recsys-examples`（commit `9a3bf5d`） | ✅ 完整获取 | `continuous.py` 全文 + 其引用的 `decode_cuda_graph.py`、`prefill_cuda_graph.py` |
| xLLM-AI `xllm`（commit `cd167a6`） | ✅ 完整获取 | `rec_master.cpp`、`fixed_steps_scheduler.cpp`、`rec_engine.cpp`、`rec_worker_impl.cpp` 关键段落 |
| `ZhiYunpenghuawei/vllm-ACS` | ⚠️ 私有仓库（GitHub 返回 404，`public_repos=1` 且不含该仓库） | 基于同团队公开设计仓库 `s13fmvp/vllm-decode-backend-design`（明确引用 `ACS_vllm-GR-main`）分析，**结论待代码验证** |

> 重要说明：vllm-ACS 的结论来自其配套公开设计文档，不是源码。若后续拿到源码，需要按同一套维度（接入方式 / 执行分组 / KV 管理 / 成图）复核本表。

---

## 1. 三个代码库的调度策略分析

### 1.1 NVIDIA `recsys-examples`：连续接入 + 阶段分组执行 + 图桶填充

**一句话**：请求来了就接入（连续批处理），但执行时按“decode 阶段 × 形状”分组，每组一个 microbatch，再用 CUDA graph 桶填充跑图。

**调度链路**（`gr_serving/continuous.py`）：

1. `submit()`：请求进入 `waiting_prefill` 队列，**不等待凑批**，即来即接；
2. `tick()`（一个逻辑调度周期）：
   - `_admit_prefill_batch()`：每 tick 从 `waiting_prefill` 接入最多 `max_prefill_batch_size` 个请求（连续接入，只是限速）；
   - `_plan_decode_batches()`：把 decoding 中的请求按 `(current_decode_step, beam_width, next_beam_width, context_len)` 分组，切成不超过 `max_decode_batch_size` 的 microbatch；
   - `_run_decode_batches()`：每组独立执行一次 `forward_decode_step`；
3. prefill 执行时再按 `input_ids.shape` 二次合批（同形状一起跑）。

**成图方式**：

- decode CUDA graph（`decode_cuda_graph.py`）：
  - 图 key = device / dtype / `beam_token_ids.shape` / `context_len` / KV shape 与 view key（地址+偏移）/ `step` / `active_beam_width` / `decode_nums`；
  - 复用条件：KV 池切片 `data_ptr + storage_offset` 完全一致，否则 pointer miss → 重捕获或退回 eager；
  - 实际 batch 小于 bucket 时，用预分配 KV 池切片 padding 到 `decode_cuda_graph_batch_buckets`，replay 后把 logits 切回实际大小；
  - 必须满足“稳定池窗口”（context/beam KV 都是池切片）。
- prefill CUDA graph（`prefill_cuda_graph.py`）：
  - 图 key = `input_ids.shape` + context KV shape/view + `last_token_logits_only`；
  - 默认 **piecewise**：embed 一张图 + 每若干层一张图 + output 一张图，降低 capture 内存；也支持整网 full 模式。

**调度与成图的关系**：调度器明确为成图服务——阶段分组保证同图内“步进一致”，bucket 填充保证“形状静态”，KV 池稳定保证“地址稳定”。

### 1.2 xLLM-AI `xllm`：固定步长批处理（调用方组 batch）

**一句话**：请求攒成一批后，作为整体完成 prefill + 固定 N 个 decode 步，批内成员全程不变；调度以“批”为粒度。

**调度链路**（`fixed_steps_scheduler.cpp`）：

1. `prepare_batch()`：
   - 从 request queue 取新请求，无 prefix cache 时展开 beam；
   - 在 `max_tokens_per_batch` / `max_seqs_per_batch` 预算内做 prefill 调度（`handle_prefill_requests`）；
   - 按 Rec 类型由 pipeline 生成 batch（`OneRec / OneRecXAttention / LlmRec / MultiRound`）；
2. `OneRecPrefillOnlyEnginePipeline::step()`（`rec_engine.cpp`）：一次 prefill forward + 连续 `kRecDecodeSteps` 次 decode forward，**整批一起走完**；
3. XAttention 变体：一个 engine step 内循环 `total_rounds` 个 round（round 间以 beam_search kernel 衔接，仍是逐 round 发 kernel，不是单图整段）。

**调度与成图的关系**：固定步长天然保证形状稳定（批不拆、步固定），因此无需复杂的“阶段分组”逻辑；代价是延迟由“攒批等待 + 组内最慢成员”决定。

### 1.3 `vllm-ACS`：vLLM 原生连续调度 + Beam 请求打包 + Backend 兼容分组（基于公开设计文档）

**一句话**：不替换 vLLM 调度内核（仍是连续接入），把“prefill + 全部 beam 后缀”打包成单一逻辑请求；执行侧按 `BatchCompatibilityKey` 分组，KV 预算前置 + 每 Backend 自管 Pool 与 GraphRunner。

**设计要点**（来自 `s13fmvp/vllm-decode-backend-design`，明确以 `ACS_vllm-GR-main` 为参考实现）：

1. **保留 vLLM 原生主链**：EngineCore、Scheduler、Executor、Worker、ModelRunner 全部保留；Beam 请求由 `_handle_beam_request_step_update()` 打包为单一逻辑 Request，带 `is_beam_decode / beam_width / beam_decode_steps / prefix_len`；
2. **Scheduler patch 最小化**：只限制 Beam 请求复用共享 Prefix，并把 `beam_data` 放进 `SchedulerOutput`；Scheduler 不解析 KV 布局；
3. **BatchCompatibilityKey 分组**：`backend_id / attention mode / beam width bucket / decode shape bucket / dtype / graph key` 相同的请求进同一 microbatch，不兼容的 Kernel 不混批；
4. **DecodeMemoryPlanner / 预算前置**：
   - 在 vLLM Paged KV 定容前划分 HBM 预算：Prompt/Paged KV、Decode Pool、Graph/Workspace、Safety Margin（10%~15%）；
   - Pool 按 bucket 受控扩容（ACS 建议 4/8/16/32）；`pool_generation` 递增会使旧图显式失效并重新 capture；
   - Graph capture 后地址必须稳定，replay 路径禁止隐式扩容；
5. **每 Backend 自管 GraphRunner**：ACS 连续 Beam KV + NPU 算子，NVIDIA BeamPath + CUDA graph；演进路径 Host Select → Device Select → FULL Graph 设备内闭环。

**调度与成图的关系**：调度只做“请求选择 + 兼容分组”，KV/图/预算全部下沉到 Backend；这是“调度策略与成图解耦”的样板——调度器不知道 KV 形状，但必须知道兼容键，才能凑出可成图的 batch。

### 1.4 三库横向对比

| 维度 | NVIDIA | xllm | vllm-ACS（设计） |
|---|---|---|---|
| 接入方式 | 连续（tick 限速接入） | 攒批后整批进入 | 连续（vLLM 原生） |
| 执行分组 | (step, beam_width, context_len) | 不分组，整批一起走 | BatchCompatibilityKey（含 shape bucket） |
| 谁负责组 batch | 引擎内自动分组 | 调用方 / 调度器攒批 | 引擎内自动分组 |
| KV 管理 | ContextKV/BeamKV Pool + BeamPath | 固定步长 KV | ACS 连续 Beam KV（bucket 池） |
| 成图 | decode/prefill graph + bucket padding | 固定形状天然利于成图 | 每 Backend GraphRunner + 预算前置 |
| 延迟特性 | 最优（即来即接） | 有攒批等待 | 最优（即来即接） |
| 吞吐特性 | 高（满负载） | 高（满负载） | 高（满负载） |
| 实现复杂度 | 中高 | 中 | 高（Backend 抽象） |

**对三者的归纳**：

- NVIDIA 和 vllm-ACS 走同一条路线：“**即来即接 + 执行侧按形状/阶段分组**”，区别只在 vllm-ACS 更进一步把 KV/Pool/Graph 下沉为可插拔 Backend；
- xllm 走另一条路线：“**固定步长 + 整批走完**”，简单、成图天然友好，但牺牲在线延迟；
- 三者都确认了一个事实：**调度必须为成图提供“同批同形状、同批同步进、KV 地址稳定”的条件**，区别只是由谁、在哪个层次保证。

---

## 2. 我们可实现的调度策略

### 2.1 策略一：调用方组 batch —— 以 batch 为粒度的调度

#### 2.1.1 定义

框架的调度单元是 **batch**，而不是单个请求：

```text
调用方（Serving / 上游）
   │  按业务/长度/优先级把请求组成 batch
   ▼
BatchSpec { batch_id, request_ids, expected_len, bucket, deadline }
   ▼
Engine：以 batch 为单位 准入 → prefill → step1 → step2 → … → done
```

调用方自己决定“哪些请求一起走”，引擎只负责把 batch 走完。这正好对应 xllm 的固定步长模型，也对应我们离线 beam search 的组批方式。

#### 2.1.2 优点

1. **成图最友好**：
   - 一个 batch 一个形状集合，图数量 = 常用 batch 形状数 × 阶段数，不会因随机到达产生形状爆炸；
   - 组内同步进，天然满足“同批同步进”的图约束；
   - 每个 batch 可以预分配一套图（prefill 图 + step1..N 图），图命中率接近 100%。
2. **调度简单、确定性高**：
   - 状态机只有 `waiting → prefill → step1…N → done`，没有逐请求的复杂预emption 纠缠（可先做到 batch 级）；
   - 端到端行为可复现，便于统计、超时、重放和回归测试。
3. **KV / 显存可预分配**：
   - 按 batch 最大长度和 beam 宽提前预留 KV 池切片与图 buffer；
   - 无请求级动态分配，OOM 风险前置到准入阶段。
4. **吞吐优势**：
   - 满批时 GPU 利用率高、kernel 大、图收益大；
   - decode 图省掉 launch 与 Python 调度开销，TBT 稳定。
5. **对调用方透明可控**：
   - 调用方掌握业务知识（长度分布、优先级、SLO），组批可以贴近业务目标。

#### 2.1.3 缺点与挑战

1. **延迟**：必须等“攒批 + 组内最慢成员”，稀疏流量下空转；
2. **长度未知**：调用方无法准确预估长度时，batch 形状失真 → bucket 浪费；
3. **组内 straggler**：一个请求异常/超长，拖累整组；
4. **对调用方负担重**：要求用户懂调度 → 这正是要做“调度插件”的原因（见 2.2）；
5. **灵活性差**：高优请求无法立刻插入，只能等下个 batch。

#### 2.1.4 后续优化点

| 优化项 | 说明 |
|---|---|
| 组窗口超时 | `max_wait_ms`：等不到满批也放行，控制延迟上界 |
| 最小/最大批大小 | 防止 1 请求成组浪费图，或组过大拖慢整体 |
| 组内长度 bucket | batch 的 `expected_len` 取组内最大，向上取整到预设 bucket |
| 组间流水 | 组 A decode 时，组 B 可以并行 prefill（利用不同阶段 kernel） |
| 组级抢占/淘汰 | 整组回 waiting，按优先级整体重排 |
| 图预热 | 启动时对常用 batch bucket 预 capture，避免首见形状抖动 |
| 组间图复用 | 同 bucket 的不同 batch 复用同一组图（图 key 不含 batch_id） |
| 动态组批建议 | 引擎按流量统计给调用方“建议 batch 大小”，弱化调用方负担 |

### 2.2 调度插件：把“用户自己的调度策略”转接为“组 batch 策略”

#### 2.2.1 为什么需要插件

直接要求调用方组 batch 太严格：调用方必须懂长度分布、流量、bucket 和图，否则组出来的 batch 形状发散，成图收益归零。

插件要解决的是：

```text
用户有自己的调度策略（FCFS / 优先级 / SLO / 长度聚类 / 业务分组 …）
        │  插件翻译
        ▼
统一的“组 batch 执行计划”（BatchSpec 流）
        │
        ▼
Engine 只认 BatchSpec，按 batch 粒度调度，不感知用户策略
```

这样**调度策略与执行机制解耦**：用户策略可以任意换，引擎侧的组 batch 执行、成图、KV 预分配完全复用。

#### 2.2.2 插件位置与边界

```text
请求流 ──► SchedulingPlugin（Host 侧，Serving 与 Engine 之间）
              │  用户策略回调：决定“下一批是谁”
              ▼
           BatchSpec 序列
              │
              ▼
        EngineCore（组 batch 执行内核）
              │
              ▼
        prefill / step1..N 图执行 → 结果回插件 → 用户拿到结果
```

边界约定：

- 插件**只做“选批”**（哪些请求、什么时候组成 batch、用哪个 bucket）；
- 插件**不做** KV 分配、图捕获、模型执行；
- Engine **只做“跑批”**，不解析用户策略；
- 插件与 Engine 通过 `BatchSpec` / `BatchResult` 契约通信。

#### 2.2.3 插件接口设计（草案）

```python
class SchedulingPlugin:
    """用户调度策略 → BatchSpec 流。"""

    def on_request_arrive(self, req: Request) -> None:
        """新请求到达，策略决定放进哪个等待组。"""

    def on_batch_finish(self, batch: BatchSpec, result: BatchResult) -> None:
        """一批结束，策略释放/回收该批资源，可能触发下一批。"""

    def on_tick(self, now: float, running: list[BatchSpec],
                waiting: list[Request]) -> None:
        """引擎空闲/周期回调，策略有机会发射新 batch。"""

    def next_batch(self, now: float) -> BatchSpec | None:
        """返回下一批（引擎空闲时调用）；None 表示暂无可发。"""


class EngineBatchInterface:
    """插件视角的引擎接口。"""

    def submit_batch(self, spec: BatchSpec) -> str: ...
        # 引擎按 spec 预分配 KV/图 bucket，开始执行
    def poll_batch(self, batch_id: str) -> BatchResult | None: ...
        # 非阻塞查询
    def available_capacity(self) -> Capacity: ...
        # 剩余 KV/图/预算，供策略决策
```

`BatchSpec` 建议字段：

```text
batch_id
request_ids
mode: prefill_only | full_generation
expected_len / bucket
beam_width_bucket
priority
deadline
graph_keys（可选，预指定的图集合）
```

#### 2.2.4 常见用户策略如何转接为组 batch

| 用户策略 | 插件转接逻辑 |
|---|---|
| FCFS | 按到达顺序凑满 bucket，满批即发；窗口超时强制发 |
| 长度聚类 | 用预估长度（或历史分布）把请求归入最近 bucket，同 bucket 凑批 |
| 优先级 | 高优请求“插队凑批”，低优让路；高优可打破满批条件单发 |
| SLO / Deadline | 每个请求带 deadline；deadline 前尽量凑批，临近 deadline 单发 |
| 业务分组（推荐业务并行度） | 业务字段相同的请求强制同批 |
| 图感知 | 只凑“已有图”的 bucket 形状，避免新 capture（见 2.2.5） |

#### 2.2.5 插件模式下的成图（策略一的图相关设计）

插件决定了图的“形状集合”，因此插件要做**图感知组批**：

1. **图注册表**：引擎维护 `bucket → graph 集合`（prefill 图 + step1..N 图 + 对应 KV 池切片）；
2. **插件决策输入**：`available_capacity` 中包含“当前可复用图 bucket 列表”；
3. **组批约束**：优先凑已有图的 bucket；确实需要新形状时，由引擎发起 capture（capture 期间该 batch 走 eager 或等待）；
4. **KV 预分配**：BatchSpec 到达即按 bucket 锁定稳定池切片，保证图 replay 地址不变；
5. **图失效**：`pool_generation` 变化（扩容/重分配）时引擎主动失效图，插件在下一次组批时避开失效 bucket 或触发重 capture；
6. **批内执行**：一个 batch 全程使用同一组图（prefill → step1 → …），批间同形状复用；
7. **预热**：插件在启动/低峰期按常见 bucket 发射“预热 batch”，把常用图全部 capture 好。

这样“调用方组 batch”就不再是负担：用户只表达业务意图，插件负责把它翻译成“形状可控、图可复用”的 batch 流。

#### 2.2.6 插件与现有 vllm 架构的接入点

候选接入位置（按改动从小到大）：

1. **Serving 层驱动（推荐先做）**：插件挂在 `vllm_gr/entrypoints/gr.py` / OpenAI serving 与 EngineCore 之间，把组好的 batch 通过现有 ADD_BATCH / BeamRequestStepUpdate 协议下发；
2. **Engine 层 batch queue**：复用 vllm `core.py` 的 `step_with_batch_queue` 思路，引擎空闲时向插件询问下一批；
3. **Scheduler 层改造**（最重，后置）：把插件作为调度策略后端，直接决定 vllm scheduler 的批次组成。

### 2.3 策略二：不组 batch、来了就处理（连续接入）+ 组 step

#### 2.3.1 子方式 A：完全即来即接（当前 vllm 现状）

每个请求按自己的节奏进 running；调度器每步按预算把 running 与 waiting 塞满。优点：延迟最低、KV 利用最充分；缺点：batch 形状随机、阶段错位，**成图最差**（图数量爆炸、命中率低）。

#### 2.3.2 子方式 B：组 step（推荐）

**定义**：接入仍是连续的，但执行按“阶段”聚合——prefill 全部完成后，把处于 **step1** 的请求组一起跑、**step2** 的组一起跑。

```text
请求即来即接入
   │
   ▼
prefill 阶段：按长度 bucket 合批（900/1000 → 1024）
   │
   ▼
decode 阶段：按 (step, beam_width, context_len) 分组
   ├─ step1 组：A、B、C
   ├─ step2 组：D、E（先到的）
   └─ …
```

这就是 NVIDIA `_plan_decode_batches` 的机制，也是 vllm-ACS 设计里 `BatchCompatibilityKey` 的用途。

**优点**：

- 延迟仍最优（不用攒批）；
- 每步的 batch 形状受控（同阶段 + 同形状）；
- 图数量 = 阶段数 × bucket 数 × beam 宽，可控；
- 不需要调用方参与，用户无感。

**成图优化**：

- prefill 图：长度 bucket + piecewise capture（见 3.3）；
- decode 图：`(step, bucket)` 二维 key，padding 到 batch bucket，KV 稳定池切片，replay 后切回；
- 首见形状才 capture，其余命中已有图。

#### 2.3.3 子方式 C：组 step + 固定 batch bucket padding

在 B 之上加一层 bucket：实际 batch 数量不足时用 KV 池切片 padding 到 1/2/4/8/16，图只按 bucket 捕获。padding 浪费与图命中率之间取平衡（见 5 的性能排序）。

#### 2.3.4 策略一与策略二的关系

两者共享同一个“**组执行内核**”（按 batch/形状执行 prefill 与各 step 图），区别只在“谁组 batch”：

```text
策略一：插件/调用方组 batch → 引擎按 BatchSpec 执行
策略二：引擎内自动组 step → 即来即接 + 阶段聚合
          ▲
          │ 同一个组执行内核
```

**落地建议**：先实现“组执行内核 + 策略二（组 step）”，再在其上加策略一的插件层。插件产出的 BatchSpec 最终也是翻译成“组 step 执行计划”，底层完全复用。

### 2.4 其他可推荐的策略

1. **混合策略（推荐主路线）**：默认策略二（连续接入 + 组 step），流量稳定/需要极致吞吐时切策略一（batch 粒度 + 插件）；
2. **固定步长 + 超时窗口**：策略一的最小可行版，先不做插件，调用方用简单 `batch_hint` 声明可组性，引擎超时放行；
3. **优先级 / SLO 感知**：高优请求插队凑批（插件层实现），或引擎内按 priority 调整组 step 顺序；
4. **预算前置 KV Pool**：借鉴 vllm-ACS 的 `DecodeMemoryPlanner`——启动时在 Paged KV 定容前划分 Decode/Graph 预算，Pool 按 bucket 扩容，`pool_generation` 使图失效重捕获；
5. **图感知组批**：调度器/插件优先凑“已有图”的 bucket，把 capture 次数作为调度指标之一；
6. **渐进式设备闭环**：Host Select → Device Select → FULL Graph（设备内回填下一轮输入），作为长期性能演进路线。

**明确不做 / 后置**：

| 项 | 原因 |
|---|---|
| 全流程一张图（prefill+全部 decode） | 依赖 host 分支（采样/停止/beam 重排/cache select），仅固定步数+全静态可行，风险高 |
| 跨阶段共享一张图 | 图 key 含 step；阶段间 KV 写入位置、decode_nums、beam/cache 选择不同 |
| PD 分离 | 需要多实例架构改造 |
| Spec decode | 需要额外 draft 模型与槽位预算 |

---

## 3. 成图设计（两种策略的公共约束）

### 3.1 硬约束

图 replay 要求：

1. **形状静态**：捕获时所有 kernel 的形状在 replay 时完全一致（batch、sequence、KV 切片尺寸）；
2. **地址稳定**：
   - 捕获期间中间张量位于图私有内存池，地址固定；
   - 输入拷进静态 buffer，输出从静态 buffer 读回；
   - KV 池切片地址必须稳定（`data_ptr + storage_offset` 一致），否则 pointer miss → 重捕获或 eager 回退；
3. **无 host 分支**：捕获代码路径与 replay 完全一致（采样/停止/beam 重排不能改路径）。

NPU/ACL 差异：

- 同样要求形状/内存大小固定；
- 允许通过“参数更新回调”（本地 `beam_attn_npu.py`）在 replay 前更新部分指针/参数，地址约束略宽松；
- capture 需要 `DeviceCaptureLock` 防并发捕获互扰。

### 3.2 decode 图

**图 key**（与 NVIDIA 对齐）：

```text
(device, dtype, batch_bucket, step, beam_width, next_beam_width,
 context_len, context_kv_shape, beam_kv_shape, kv_view_key, decode_nums)
```

**执行流程**：

1. 调度器/插件按阶段与形状产出 microbatch；
2. 实际 batch → bucket（向上取整）；
3. KV 池切片 padding 到 bucket（context/beam 都要稳定切片）；
4. 拷贝 `beam_token_ids` / `topk_indices` 进静态输入 buffer；
5. 图 replay → logits 写共享输出 buffer；
6. 按实际 batch 切回 logits；
7. 记录 miss reason（shape / context_key / beam_key / pool_window 等）。

### 3.3 prefill 图

两种方案：

1. **长度 bucket 方案（推荐起步）**：`input_ids` 形状固定为 `[B, bucket_len]`，900/1000 padding 到 1024；
   - 代价：padding token 的 attention 计算浪费（900→1024 约 12%，取决于分布）；
2. **max-token 单图方案（更 vllm）**：捕获一张按 `max_num_batched_tokens` 预算的图，真实长度作为数据传入 attention metadata；
   - 前提：prefill kernel 支持变长（flattened tokens + metadata）。

捕获方式：默认 **piecewise**（embed 图 + 每 4 层一张图 + output 图），降低 capture 内存；整网图作为 fallback。

### 3.4 成图层次可行性总表

| 层次 | 可行性 | 依据 | 代价/风险 |
|---|------|------|----------|
| 每 decode 阶段一张图 | ✅ 可行（现状） | NVIDIA key 含 step；本地已逐阶段成图 | 图数 = 阶段 × bucket |
| 同阶段并行 decode 一张图 | ✅ 可行 | NVIDIA 同 (step, beam, ctx_len) 合批 + bucket padding | padding 浪费、需稳定池 |
| 不同阶段请求一张图 | ❌ 不做 | key 含 stage；阶段间逻辑不同 | — |
| prefill 单独成图 | ✅ 可行 | NVIDIA full/piecewise 均已实现 | 长度 bucket 或需变长 kernel |
| 全流程一张图 | ⚠️ 后置研究 | 需固定步数 + 无 host 分支 + 全静态 | 高复杂度、极脆 |

### 3.5 两种策略下的图布局对比

| | 策略一（调用方组 batch） | 策略二（组 step） |
|---|---|---|
| 谁决定形状 | 插件/调用方 | 引擎内自动聚合 |
| 图数量 | 常用 batch 形状 × 阶段 | 阶段 × bucket × beam |
| 图命中率 | 接近 100%（形状由插件控制） | 高（聚合可控） |
| capture 控制 | 插件避开新形状，预热常用形状 | 首见形状才 capture |
| KV 预分配 | BatchSpec 到达即锁定 | 池稳定窗口 |
| 延迟 | 有攒批等待 | 最优 |

---

## 4. 实施路线图

> 周期为估计值，按 CUDA 先行、NPU 跟进估算。

### Phase 0：基线与指标（约 1–2 周）

- 建立 benchmark：吞吐（req/s、token/s）、FTT、TBT、KV 利用率、图命中率、capture 内存；
- 抓流量分布：context 长度分布、阶段并发、到达间隔；
- 确定 bucket 初值（3–5 个 decode bucket、2–3 个 prefill bucket）。

**验收**：基线可复现，指标埋点齐全。

### Phase 1：组执行内核 + 组 step（策略二主体，约 2–3 周）

- 调度输出按 `(stage, bucket)` 分组（复用 `beam_decode_steps`）；
- prefill 按长度 bucket 合批，decode 按 step 合批；
- 保持 token 预算/KV 分配语义不变。

**验收**：同批请求中不同阶段的请求不再混跑；与基线输出一致。

### Phase 2：decode 图 bucket + KV 稳定池（约 3–4 周）

- decode 图 runner（CUDA 先做，ACL 跟进）：key、padding、共享 logits buffer、miss 统计；
- KV 分配器支持稳定池窗口（按 bucket 预留 slot 区）；
- 动态跳过（pool 不稳定、padding 池不足时回退 eager）。

**验收**：命中率 ≥ 90%（典型负载）；padding 浪费有账可查；TBT 下降。

### Phase 3：prefill 图（约 2–3 周）

- 长度 bucket + piecewise capture（CUDA）；
- 评估 max-token 单图（kernel 支持变长则替换）；
- NPU 侧 ACL prefill 图。

**验收**：FTT 改善可量化；capture 内存受控。

### Phase 4：调度插件框架 + 策略一（约 3–4 周）

- `SchedulingPlugin` 接口 + `BatchSpec` 契约；
- Serving 层接入（插件挂载在 gr.py / OpenAI serving 与 Engine 之间）；
- 内置插件示例：FCFS 聚合、长度聚类、优先级、deadline；
- 图感知组批：插件读取可用 bucket，避免新 capture。

**验收**：同一组执行内核可通过插件切换策略；插件产出的 batch 图命中率 ≥ 90%。

### Phase 5：优先级 / 预算前置 / 组级抢占（约 2–3 周）

- 优先级与 SLO 感知（插件层 + 引擎层）；
- DecodeMemoryPlanner 预算前置（借鉴 vllm-ACS 设计）；
- 组级抢占与 `pool_generation` 图失效机制。

**验收**：高优请求延迟达标；预算不超；图失效/重捕获可观测。

### Phase 6：压测与调优（持续）

- bucket 数量、padding 比例、capture 预热、图 LRU 上限；
- 多模型（RECIF 3-token / 更长 decode / catalog-constrained）压测；
- 策略一 vs 策略二 A/B 数据结论。

---

## 5. 预期性能排序

> 相对值（以“纯连续接入、无分组、无图”为基线 1.0），为目标方向而非实测承诺。

| 策略组合 | 吞吐 | FTT | TBT | 图命中率 | padding 浪费 | 实现成本 |
|---|---|---|---|---|---|---|
| 策略一（调用方组 batch + 图，满流量） | ★★★★★ | ★★★ | ★★★★★ | 高 | 中 | 中 |
| 策略二 B（连续接入 + 组 step + 图） | ★★★★★ | ★★★★★ | ★★★★★ | 高 | 中 | 中高 |
| 策略二 C（组 step + bucket padding） | ★★★★★ | ★★★★★ | ★★★★★ | 很高 | 中高 | 中高 |
| 策略一（稀疏流量） | ★★★ | ★★ | ★★★★ | 中 | 高 | 中 |
| 混合（默认二、高峰切一） | ★★★★★ | ★★★★★ | ★★★★★ | 高 | 中 | 高 |
| 纯即来即接（当前 vllm 基线） | ★★★★ | ★★★★ | ★★★ | 低 | 0 | 0 |
| 纯静态批处理 | ★★ | ★★ | ★★★ | 中 | 高 | 低 |

**判断口径**：

- **延迟**：策略二最优（无需攒批）；策略一在满流量时接近，稀疏流量明显下降；
- **吞吐**：满负载、形状集中时策略一与策略二 C 接近；策略二 B 在形状分散时略低；
- **成图收益**：decode 图主要压 TBT；prefill 图压 FTT（占比小，prefill 计算密集）；
- **图数量/内存**：阶段 × bucket × beam 是主要开销，bucket 数量必须克制；
- **推荐组合**：Phase 1–3 做策略二；Phase 4 做插件后，把策略一作为“高吞吐模式”在高峰启用，最终形成混合策略。

---

## 6. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 图私有池内存膨胀 | 显存压力 | 控制 bucket 数；图 LRU 上限；piecewise；监控 capture 内存 |
| capture 抖动（首见新形状） | 单步延迟尖峰 | 启动/低峰预热；插件避开新形状 |
| padding 浪费 | 有效吞吐下降 | 按流量分布选 bucket；变长 kernel 后改 max-token 单图 |
| 组 step 后 batch 利用率下降 | 吞吐损失 | bucket 调优；高峰切策略一 |
| 插件组批不合理（形状发散） | 图命中率下降 | 图感知组批；引擎返回可用 bucket 列表；插件准入校验 |
| 攒批等待过长（策略一） | FTT 劣化 | 窗口超时强制放行；deadline 感知单发 |
| KV 池不稳定导致 pointer miss | 图命中率下降 | 稳定池窗口；`pool_generation` 显式失效；miss reason 埋点 |
| NPU ACL 捕获并发互扰 | capture 失败/卡顿 | DeviceCaptureLock；捕获与执行流分离 |
| 优先级抢占破坏图复用 | 命中率下降 | 被抢占请求回原池窗口；整组回 waiting 后按原 bucket 重发 |
| vLLM 版本升级破坏 patch | 接入点失效 | 版本能力检查；patch 集中管理（vllm-ACS 设计同样提醒） |

---

## 7. 参考代码与文档位置

### 7.1 已抓取的源码

- NVIDIA `continuous.py`：`GRContinuousScheduler.tick` / `_admit_prefill_batch` / `_plan_decode_batches`（L270–L434、L515–L545）、`_run_decode_batch`（L2014–L2153）、`_decode_cuda_graph_inputs`（L2335 起）
- NVIDIA `decode_cuda_graph.py`：图 key / padding / pointer reuse
- NVIDIA `prefill_cuda_graph.py`：长度 key / piecewise / full
- xllm `fixed_steps_scheduler.cpp` L193–L321：`prepare_batch` 攒批
- xllm `rec_engine.cpp` L699–L759：OneRec prefill + `kRecDecodeSteps`
- xllm `rec_worker_impl.cpp` L1741–L1959：multi-round beam 循环
- vllm `scheduler.py` L438–L790：连续调度
- vllm `core.py` L580–L608：schedule → execute → update
- 本地 `vllm_gr/v1/engine/core.py` L195–L227：Beam Request / decode stage

### 7.2 vllm-ACS 公开设计仓库（私有源码的替代依据）

- `s13fmvp/vllm-decode-backend-design`（已抓取到 `/tmp/vllm_acs_design/`）：
  - `docs/simple-design.md`：ACS 连续 KV vs NVIDIA BeamPath 的区别
  - `docs/full-design.md`：BatchCompatibilityKey / DecodeMemoryPlanner / BackendPoolPolicy / Graph 失效
  - `docs/vllm-gr-based-architecture.md`：在 vllm-gr 上的最小接入路径

---

## 附录 A：指标定义

| 指标 | 定义 |
|---|---|
| FTT（首 token 延迟） | 请求接入到第一个生成 token 输出的时间 |
| TBT（token 间延迟） | decode 相邻两步间隔（含调度/执行/同步） |
| 图命中率 | 图命中次数 / 总 forward 次数 |
| pointer miss 率 | 形状命中但地址不匹配占比 |
| padding 浪费 | padding token 数 / 图内总 token 数 |
| capture 内存 | 图私有池 + 共享输出 buffer 总量 |
| KV 利用率 | 已用 KV 块 / 总 KV 块 |
| 攒批等待 | 请求进入插件到 batch 放行的平均时间（策略一） |
| 组利用率 | 实际 batch 大小 / bucket 大小（策略一/二 C） |

## 附录 B：一句话结论

**先做“组执行内核 + 组 step”（策略二），保证即来即接与成图；再加调度插件，把用户策略翻译成 BatchSpec（策略一），使框架真正以 batch 为粒度调度。两个策略共享同一内核，差别只在“谁组 batch”。**
